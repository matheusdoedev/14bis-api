const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "src", "database", "config.js"),
  "models-path": path.resolve(__dirname, "src", "app", "models"),
  "migrations-path": path.resolve(__dirname, "src", "database", "migrations"),
};
